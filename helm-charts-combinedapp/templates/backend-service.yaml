apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  type: {{ .values.backend.service.type }}
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: {{ .values.backend.service.port }}

